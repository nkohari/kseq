{
  "name": "lseq",
  "version": "0.1.0",
  "description": "an ordered vector CRDT based on LSEQ",
  "main": ".build/index.js",
  "dependencies": {
    "lodash": "^4.7.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "ts-node": "^0.7.1",
    "typescript": "^1.8.7"
  },
  "scripts": {
    "test": "node_modules/.bin/mocha --compilers ts:ts-node/register test/**/*.ts"
  }
}
